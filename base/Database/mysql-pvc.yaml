apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql
  namespace: loginapp-dev
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs-dynamic