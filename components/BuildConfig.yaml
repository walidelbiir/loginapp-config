apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: login-app-build
spec:
  output:
    to:
      kind: ImageStreamTag
      name: login-app:latest
  source:
    type: Git
    git:
      uri: 'https://github.com/walidelbiir/redhat-loginapp.git'
      ref: "main"
  strategy:
    type: Docker
    dockerStrategy:
      dockerfilePath: Dockerfile-redhat  
  triggers:
    - type: ConfigChange
    